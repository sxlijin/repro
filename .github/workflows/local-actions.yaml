name: Run actions locally

on:
  workflow_dispatch: {}

jobs:

  check:
    runs-on: ubuntu-latest
    name: Trunk Check Runner

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          path: foo

      - name: symlink
        shell: bash
        run: ln -s foo foo-link

      - uses: ./foo-link/node-action